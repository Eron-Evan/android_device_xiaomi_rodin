<?xml version="1.0" encoding="utf-8"?>
<!--
     Copyright (C) 2023 Paranoid Android

     SPDX-License-Identifier: Apache-2.0
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.xiaomi.settings"
    android:sharedUserId="android.uid.system">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACCESS_SURFACE_FLINGER" />
    <uses-permission android:name="android.permission.INTERACT_ACROSS_USERS_FULL" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" />
    <uses-permission android:name="android.permission.WRITE_SECURE_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />

    <application
        android:label="@string/app_name"
        android:persistent="true"
        android:defaultToDeviceProtectedStorage="true"
        android:directBootAware="true">

        <receiver
            android:name=".BootCompletedReceiver"
            android:exported="true">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <service
            android:name=".display.ColorService"
            android:exported="false">
        </service>

        <!-- Services -->
        <service android:name=".battery.ChargingLimitService" />

        <!-- Battery/Charging Activity — Get called by XiaomiPartsFragment -->
        <activity
            android:name=".battery.ChargingControlActivity"
            android:label="@string/charging_control_title"
            android:exported="false" />

        <!-- ====================================================== -->
        <!-- Xiaomi Parts Hub — Standalone launcher entry point -->
        <!-- ====================================================== -->
        <activity
            android:name=".hub.XiaomiPartsActivity"
            android:label="@string/app_name"
            android:icon="@drawable/ic_xiaomiparts"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS" />
            </intent-filter>
            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.system" />
            <meta-data
                android:name="com.android.settings.icon"
                android:resource="@drawable/ic_xiaomiparts" />
            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/xiaomi_parts_summary" />
        </activity>

        <!-- ====================================================== -->
        <!-- GameBar — Get called by XiaomiPartsFragment        -->
        <!-- ====================================================== -->
        <activity
            android:name=".gamebar.GameBarSettingsActivity"
            android:label="@string/game_bar_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="false" />

        <!-- GameBar AppSelector -->
        <activity android:name=".gamebar.GameBarAppSelectorActivity" />
        <activity android:name=".gamebar.GameBarAppRemoverActivity" />

        <!-- GameBar Tile Service -->
        <service
            android:name=".gamebar.GameBarTileService"
            android:label="@string/game_bar_tile_label"
            android:icon="@drawable/ic_gamebar"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <!-- GameBar Monitor Service -->
        <service
            android:name=".gamebar.GameBarMonitorService"
            android:exported="false" />

        <!-- GameBar BootReceiver -->
        <receiver
            android:name=".gamebar.GameBarBootReceiver"
            android:exported="true"
            android:enabled="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- ====================================================== -->
        <!-- Touch Sampling — Get called by XiaomiPartsFragment -->
        <!-- ====================================================== -->
        <activity
            android:name=".touchsampling.TouchSamplingSettingsActivity"
            android:label="@string/htsr_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="false" />

        <service
            android:name=".touchsampling.TouchSamplingService"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:exported="false" />

        <activity android:name=".touchsampling.TouchSamplingAppSelectorActivity" />

        <!-- Touch Sampling Tile Service -->
        <service
            android:name=".touchsampling.TouchSamplingTileService"
            android:label="@string/touch_sampling_tile_label"
            android:icon="@drawable/ic_touch_sampling_tile"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <!-- ====================================================== -->
        <!-- Core Control — Get called by XiaomiPartsFragment   -->
        <!-- ====================================================== -->
        <activity
            android:name=".corecontrol.CoreControlActivity"
            android:label="@string/core_control_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="true" />


        <!-- ====================================================== -->
        <!-- Resolution — per-app + system-wide + QS tile + boot    -->
        <!-- ====================================================== -->
        <activity
            android:name=".resolution.ResolutionActivity"
            android:label="@string/upscale_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="false" />

        <activity
            android:name=".resolution.SystemResolutionActivity"
            android:label="@string/system_resolution_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="false" />

        <service
            android:name=".resolution.ResolutionService"
            android:exported="false" />

        <receiver
            android:name=".resolution.ResolutionBootReceiver"
            android:directBootAware="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Resolution QS Tile -->
        <service
            android:name=".resolution.ResolutionQsTileService"
            android:label="@string/resolution_qs_label"
            android:icon="@drawable/ic_resolution"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>

        <!-- ====================================================== -->
        <!-- Clear Speaker — injected into Settings > Sound          -->
        <!-- ====================================================== -->
        <activity
            android:name=".speaker.ClearSpeakerActivity"
            android:label="@string/clear_speaker_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="true" />

        <activity
            android:name=".refreshrate.RefreshActivity"
            android:label="@string/refresh_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="false" />

        <!-- Service for refresh rate management -->
        <service
            android:name=".refreshrate.RefreshService"
            android:exported="true"
            android:permission="RefreshService">
        </service>

        <!-- ====================================================== -->
        <!-- Auto HBM / HBM — activity, service, QS tiles, boot     -->
        <!-- ====================================================== -->
        <!-- TileHandler activity -->
        <activity
            android:name=".TileHandlerActivity"
            android:exported="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.NoDisplay">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
        </activity>
        <!-- Auto HBM activity -->
        <activity
            android:name=".autohbm.AutoHbmActivity"
            android:label="@string/auto_hbm_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="true">
            <intent-filter>
                <action android:name="com.android.settings.action.IA_SETTINGS" />
            </intent-filter>
            <meta-data
                android:name="com.android.settings.category"
                android:value="com.android.settings.category.ia.display" />
            <meta-data
                android:name="com.android.settings.summary"
                android:resource="@string/auto_hbm_summary" />
        </activity>
        <!-- Auto HBM service -->
        <service
            android:name=".autohbm.AutoHbmService"
            android:exported="false" />
        <!-- Auto HBM tile service -->
        <service
            android:name=".autohbm.AutoHbmTileService"
            android:label="@string/auto_hbm_title"
            android:icon="@drawable/ic_auto_hbm_tile"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>
        <!-- HBM tile service -->
        <service
            android:name=".autohbm.HbmTileService"
            android:label="@string/tile_hbm"
            android:icon="@drawable/ic_hbm_tile"
            android:exported="true"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
            <meta-data
                android:name="android.service.quicksettings.TOGGLEABLE_TILE"
                android:value="true" />
        </service>
        <!-- Startup — Auto HBM visszaállítása bootkor -->
        <receiver
            android:name=".Startup"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- About Me -->
        <activity
            android:name=".aboutme.AboutMeActivity"
            android:label="@string/about_me_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="false" />

        <!-- Visual Enhancement (MiraVision PQ) -->
        <activity
            android:name=".pq.VisualEnhancementActivity"
            android:label="@string/pq_title"
            android:theme="@style/Theme.SubSettingsBase"
            android:exported="false" />

    </application>
</manifest>
